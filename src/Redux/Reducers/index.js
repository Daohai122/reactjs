import { combineReducers } from "redux";
import {ChangeMennu} from "./ChangUrlReducer";

 const allReducers = combineReducers({
    ChangeMennu,
     // them reducer vao day
 });
 export default allReducers;